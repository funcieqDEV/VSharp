set a = [1, 2, 3]

println(a)
func foo(age) {
  set result = if (age >= 18) {
    "nice you are an adult"
  }
  else {
    "you are not an adult"
  }
  println(result)
}

set age = input(">")
set age = int(age)
foo(age)

